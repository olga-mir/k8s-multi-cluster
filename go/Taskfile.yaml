version: '3'

tasks:
  build:
    cmds:
      - go build -v ./cmd/multicluster-demo
    desc: Build the multicluster-demo application

  run:
    cmds:
      - go run ./cmd/multicluster-demo
    desc: Run the multicluster-demo application

  test:
    cmds:
      - go test -v ./pkg/...
    desc: Run all tests in the pkg directory

